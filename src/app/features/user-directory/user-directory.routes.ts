import { Routes } from '@angular/router';
import { UserDirectoryPage } from './pages/user-directory-page/user-directory-page';

export const USER_DIRECTORY_ROUTES: Routes = [
  {
    path: '', 
    component: UserDirectoryPage
  }

];